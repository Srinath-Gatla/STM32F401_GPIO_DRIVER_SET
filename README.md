# STM32F401_GPIO_DRIVER_SET
Custom bare-metal GPIO driver for STM32F401RE (ARM Cortex-M4). Direct register-level programming without HAL. Supports clock enable, pin configuration, port read/write, and binary counter demo using push button and 8 LEDs. Designed for embedded systems learning and driver development.
